scrollReveal({
  elem: ".sr_upcoming_shows",
  duration: 1.2,

  newStyle: {
    opacity: "1",
  },
});
scrollReveal({
  elem: ".sr_gallery",
  duration: 1,

  newStyle: {
    opacity: "1",
  },
});
scrollReveal({
  elem: ".sr_live_now",
  duration: 1.2,

  newStyle: {
    opacity: "1",
  },
});
scrollReveal({
  elem: ".sr_contact_us",
  duration: 1.2,

  newStyle: {
    opacity: "1",
  },
});

//slides
scrollReveal({
  elem: ".sr_latest_release_desc",
  duration: 1.2,

  newStyle: {
    opacity: "1",
  },
});
scrollReveal({
  elem: ".sr_latest_release_port",
  distance: -200,
  duration: 1,

  newStyle: {
    opacity: "1",
    transform: "translateY(0)",
  },
});

scrollReveal({
  elem: ".sr_recent_posts_h1",
  duration: 1.2,

  newStyle: {
    opacity: "1",
  },
});
scrollReveal({
  elem: ".sr_recent_posts_port",
  distance: -200,
  duration: 1,

  newStyle: {
    opacity: "1",
    transform: "translateY(0)",
  },
});

// elem,
// distance,
// duration,
// delay,
// newClass,
// newStyle: newStyleObj,
